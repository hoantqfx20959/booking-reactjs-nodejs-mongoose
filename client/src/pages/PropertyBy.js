import Frame from '../components/UI/Frame/Frame';

import CountBy from '../components/home/CountBy';

const PropertyBy = () => {
  return (
    <Frame>
      <CountBy />
    </Frame>
  );
};

export default PropertyBy;
